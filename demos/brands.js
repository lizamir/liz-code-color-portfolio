const reducedMotion=matchMedia('(prefers-reduced-motion: reduce)').matches;
document.querySelectorAll('[data-carousel]').forEach(el=>{const slides=[...el.querySelectorAll(':scope > img')];let index=0,paused=reducedMotion;const counter=el.querySelector('[data-counter]'),pause=el.querySelector('[data-pause]');const show=n=>{index=(n+slides.length)%slides.length;slides.forEach((s,i)=>{s.classList.toggle('active',i===index);s.setAttribute('aria-hidden',String(i!==index))});counter.textContent=String(index+1).padStart(2,'0')+' / '+String(slides.length).padStart(2,'0')};const updatePause=()=>{pause.textContent=paused?'▶':'⏸';pause.setAttribute('aria-label',paused?'הפעלת החלפת תמונות':'השהיית החלפת תמונות');pause.setAttribute('aria-pressed',String(paused))};el.querySelector('[data-prev]').addEventListener('click',()=>show(index-1));el.querySelector('[data-next]').addEventListener('click',()=>show(index+1));pause.addEventListener('click',()=>{paused=!paused;updatePause()});let onScreen=false;new IntersectionObserver(entries=>onScreen=entries[0].isIntersecting).observe(el);setInterval(()=>{if(!paused&&onScreen&&!document.hidden&&!el.matches(':hover')&&!el.contains(document.activeElement))show(index+1)},5200);show(0);updatePause()});
document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{const filter=button.dataset.filter;document.querySelectorAll('[data-filter]').forEach(b=>{b.classList.toggle('active',b===button);b.setAttribute('aria-pressed',String(b===button))});let count=0;document.querySelectorAll('[data-category]').forEach(card=>{card.hidden=filter!=='all'&&card.dataset.category!==filter;if(!card.hidden)count++});document.querySelector('.filter-status').textContent=count+' טעמים'}));
const scoops=[];const scoopList=document.querySelector('.scoop-list');const scoopStatus=document.querySelector('#scoop-status');function renderScoops(){scoopList.replaceChildren();if(!scoops.length){const p=document.createElement('p');p.textContent='הגביע שלכם מחכה לטעם הראשון.';scoopList.append(p)}scoops.forEach((name,i)=>{const row=document.createElement('div');row.className='scoop-item';const label=document.createElement('span');label.textContent=String(i+1).padStart(2,'0')+' / '+name;const remove=document.createElement('button');remove.textContent='×';remove.setAttribute('aria-label','הסרת '+name);remove.addEventListener('click',()=>{scoops.splice(i,1);renderScoops();scoopStatus.textContent='הטעם הוסר מהגביע'});row.append(label,remove);scoopList.append(row)})}
document.querySelectorAll('[data-scoop]').forEach(button=>button.addEventListener('click',()=>{if(scoops.length<3){scoops.push(button.dataset.scoop);renderScoops();scoopStatus.textContent='נוסף לגביע: '+button.dataset.scoop}else{scoopStatus.textContent='הגביע מלא! אפשר להסיר טעם ולהחליף אותו.'}document.querySelector('#mix').scrollIntoView({behavior:reducedMotion?'instant':'smooth',block:'center'})}));document.querySelector('#clear-scoops')?.addEventListener('click',()=>{scoops.length=0;renderScoops();scoopStatus.textContent='אפשר לבחור שילוב חדש.'});
const burgers={classic:['THE ORIGINAL','הקלאסי','קציצת בקר, חסה, עגבנייה, מלפפון חמוץ ובצל. כל מה שצריך לביס טוב.','../assets/burger-detail.webp'],hot:['TURN UP THE HEAT','החריף','קציצת בקר, פלפל חריף, בצל ורוטב עם אופי. למי שאוהב שהביס נותן קצת יותר.','../assets/burger.webp'],crunch:['CRUNCH TIME','הקראנצ׳י','קציצת בקר, בצל פריך וחסה. שכבות של מרקם, מהביס הראשון ועד האחרון.','../assets/burger-detail.webp']};let currentBurger='classic';const summary=document.querySelector('#burger-summary');function updateBurger(){if(!summary)return;const additions=[...document.querySelectorAll('.build-controls input:checked')].map(i=>i.value);summary.textContent=burgers[currentBurger][1]+' · '+(additions.join(' + ')||'בלי תוספות')}
document.querySelectorAll('[data-burger]').forEach(button=>button.addEventListener('click',()=>{currentBurger=button.dataset.burger;document.querySelectorAll('[data-burger]').forEach(b=>{b.classList.toggle('active',b===button);b.setAttribute('aria-pressed',String(b===button))});const data=burgers[currentBurger];document.querySelector('#burger-kicker').textContent=data[0];document.querySelector('#burger-title').textContent=data[1]+'.';document.querySelector('#burger-description').textContent=data[2];document.querySelector('#burger-image').src=data[3];document.querySelector('#burger-image').alt=data[1];updateBurger()}));document.querySelectorAll('.build-controls input').forEach(input=>input.addEventListener('change',updateBurger));document.querySelector('#copy-burger')?.addEventListener('click',async()=>{const status=document.querySelector('#burger-copy-status');try{await navigator.clipboard.writeText(summary.textContent);status.textContent='ההרכב שלכם הועתק.'}catch{status.textContent='אפשר לסמן את ההרכב שמופיע מעל ולהעתיק אותו.'}});
